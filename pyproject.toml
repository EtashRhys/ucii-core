[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "ucii-core"
version = "0.1.0"
description = "Universal Cryptographic Identity Infrastructure Core"
readme = "README.md"
requires-python = ">=3.11"
license = {file = "LICENSE"}

authors = [
    {name = "UCII"}
]

dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
